#!/usr/bin/env ruby
$LOAD_PATH.unshift(File.expand_path(File.join(File.dirname(File.realpath(__FILE__)), "..", "lib")))

puts $LOAD_PATH

require 'rubygems'
require 'conveyor'

# Ensure STDOUT is flushed
STDOUT.sync = true

Conveyor.start
